<% include ../partials/header %>

<div class="search-bar" style="z-index:1;">
	<input type="text" id="searchField"/>
	<button id="searchButton">Search</button>
	<div class="dropdown">
		<span class="filterLabel">Type:</span>
		<input type="button" id="dropButton" class="dropbtn" value="Any"/>
		<div id="dropContent" class="dropdown-content">
			<button class="filter-button">Any</button>
			<% workTypes.forEach(function(workType) { %>
				<button class="filter-button"><%= workType.type %></button>
			<% }); %>
		</div>
	</div>
</div>

<div class="container-fluid row" style="display: flex; flex-wrap: wrap;z-index:-1;">
	<% works.forEach(function(work) { %>
		<div class="col-sm-3">
			<div class="thumbnail">
				<a class="darken" href="/works/<%= work.id %>"><img height src="<%= work.image %>"/></a>
				<div class="preview-title-content">
					<span class="preview-title"><%= work.title %></span>
				</div>
			</div>
		</div>
	<% }); %>
</div>
